# Generated Helm repo contents

This folder is used as the GitHub Pages source for the Helm repository.

It will contain:
- `index.yaml`
- packaged chart archives such as `app-template-1.0.0.tgz`

The contents are updated by `.github/workflows/publish-chart.yml`.
